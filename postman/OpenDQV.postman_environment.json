{
  "name": "OpenDQV Local",
  "values": [
    {
      "key": "base_url",
      "value": "http://localhost:8000",
      "type": "default",
      "enabled": true,
      "description": "OpenDQV API base URL. Change to http://localhost:8080 when using the demo Docker Compose."
    },
    {
      "key": "auth_token",
      "value": "",
      "type": "secret",
      "enabled": true,
      "description": "Personal Access Token. Leave blank when AUTH_MODE=open (default). Generate via POST /api/v1/tokens/generate when AUTH_MODE=token."
    },
    {
      "key": "contract_name",
      "value": "customer",
      "type": "default",
      "enabled": true,
      "description": "Default contract name used in contract detail, explain, history, lifecycle, code generation, and registry requests. Change to any active contract name."
    }
  ],
  "_postman_variable_scope": "environment",
  "schema": "https://schema.getpostman.com/json/collection/v2.1.0/environment.json"
}
