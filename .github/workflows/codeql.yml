name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Weekly on Monday at 08:00 UTC
    - cron: '0 8 * * 1'

permissions: read-all

jobs:
  analyze:
    name: Analyze (Python)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6

      - name: Initialize CodeQL
        uses: github/codeql-action/init@dfad8f8ebc168f044d22d94180b54591ef6c0321  # v3
        with:
          languages: python

      - name: Autobuild
        uses: github/codeql-action/autobuild@dfad8f8ebc168f044d22d94180b54591ef6c0321  # v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@dfad8f8ebc168f044d22d94180b54591ef6c0321  # v3
        with:
          category: '/language:python'
